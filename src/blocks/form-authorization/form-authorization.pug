div.form-authorization
	form.form-authorization__forma(metod="post")
		div.form-control
			include ../input/input.pug
			label.visually-hidden(for="login") Login
			+input({type: 'text', name: 'login', placeholder: 'Login'})

		div.form-control
			label.visually-hidden(for="password") Password
			+input({type: 'password', name: 'password"', placeholder: 'Password'})(class="input--password")

		div.form-authorization__btn
			include ../btn/btn.pug
			+btn({text: 'Enter', type: 'submit'})(class="btn--block")

	div.form-authorization__links
		p or 
			a.form-authorization__link(href="#") sign up 
			|fo free
		p
			a.form-authorization__link(href="#") Forgot your password?
